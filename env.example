# Copy to .env for local notes only — these are passed to AWS as SAM
# parameters (see README), not loaded from a file at runtime.

TELEGRAM_BOT_TOKEN=123456:abc-your-botfather-token
ALLOWED_CHAT_ID=
PARTNER_ACCESS_KEY=choose-a-long-random-string
PUBLIC_SITE_URL=https://cannygreen.ie
